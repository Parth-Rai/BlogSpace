<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Blogs - BlogSpace</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/layout.css">
</head>
<body>
    <%- include('partials/header', { user: locals.user }) %>

    <div class="blog-section container-lg" style="min-height: 70vh;">
      <h2 id="blog-section" class="section-title py-5">All Blog Posts</h2>
      <div class="blog-list">
        <% if (locals.blogs && blogs.length > 0) { %>
          <% blogs.forEach(blog => { %>
            <div class="blog-card">
              <a href="/blogs/<%= blog.id %>" style="text-decoration: none; color: inherit;">
                <h5 class="headings"><%= blog.title %></h5>
              </a>
              <p><%= blog.content.substring(0, 200) %>...</p> <small>By: <%= blog.author_email %> on <%= new Date(blog.created_at).toLocaleDateString() %></small>
            </div>
          <% }) %>
        <% } else { %>
          <p>No blog posts have been written yet.</p>
        <% } %>
      </div>
    </div>

    <footer class="mt-4 text-black text-center">
      <p>Parth Rai @<%= currentYear %>.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>