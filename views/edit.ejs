<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit: <%= blog.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/layout.css">
</head>
<body>
    <%- include('partials/header', { user: locals.user }) %>

    <div class="container-lg" style="min-height: 70vh;">
        <h2 class="form-heading pt-5">Edit Blog Post</h2>
        <form action="/blogs/<%= blog.id %>/edit" method="POST" class="blog-form mt-4">
            <div class="mb-3">
                <label for="title" class="headings pb-2">Title</label>
                <input type="text" id="title" name="title" required class="form-control" value="<%= blog.title %>">
            </div>
            <div class="mb-3">
                <label for="content" class="headings pb-2">Content</label>
                <textarea id="content" name="content" rows="15" required class="form-control"><%= blog.content %></textarea>
            </div>
            <button type="submit" class="btn btn-submit">Save Changes</button>
        </form>
    </div>

    <footer class="mt-4 text-black text-center">
        <p>Parth Rai @<%= currentYear %>.</p>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>