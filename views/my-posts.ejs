<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Posts - BlogSpace</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/layout.css">
</head>
<body>
    <%- include('partials/header', { user: locals.user }) %>

    <div class="container-lg" style="min-height: 70vh;">
      <h2 class="section-title py-5">Manage Your Posts</h2>
      
      <% if (locals.posts && posts.length > 0) { %>
        <% posts.forEach(post => { %>
          <div class="blog-card d-flex justify-content-between align-items-center">
            <div>
              <a href="/blogs/<%= post.id %>" style="text-decoration: none; color: inherit;">
                <h5 class="headings mb-0"><%= post.title %></h5>
              </a>
              <small class="text-muted">Published on <%= new Date(post.created_at).toLocaleDateString() %></small>
            </div>
            <div class="post-actions">
              <a href="/blogs/<%= post.id %>/edit" class="btn btn-sm btn-secondary">Edit</a>
              <form action="/blogs/<%= post.id %>/delete" method="POST" class="d-inline ms-1">
                <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure?');">Delete</button>
              </form>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p>You haven't written any posts yet. <a href="/blogs/new">Write your first one!</a></p>
      <% } %>
    </div>

    <footer class="mt-4 text-black text-center">
      <p>Parth Rai @<%= currentYear %>.</p>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>